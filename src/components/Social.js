const Social = ({ imageUrl }) => {
  return (
    <a href="#" target="_blank" rel="noopener noreferrer" className="social">
      <img src={imageUrl} alt="Socials" />
    </a>
  );
};

export default Social;
